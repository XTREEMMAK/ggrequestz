# Docker Build Environment Variables
# This file contains safe placeholder values used during Docker build process
# Real environment variables are provided at runtime via docker-compose.yml

# =============================================================================
# BUILD-TIME PLACEHOLDERS
# These are safe values used only during `npm run build` in Docker
# Real values are injected at runtime via Docker Compose environment
# =============================================================================

# Authentication Method
AUTH_METHOD=authentik

# Authentication Placeholders (build-time safe values)
AUTHENTIK_CLIENT_ID=build-placeholder-client-id
AUTHENTIK_CLIENT_SECRET=build-placeholder-client-secret
AUTHENTIK_ISSUER=https://auth.example.com/application/o/app

# Session Security (build-time placeholder - must be at least 32 chars)
SESSION_SECRET=build-placeholder-session-secret-minimum-32-characters-long

# IGDB API (build-time placeholders)
IGDB_CLIENT_ID=build-placeholder-igdb-client
IGDB_CLIENT_SECRET=build-placeholder-igdb-secret

# Database Configuration (build-time placeholders)
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=ggrequestz
POSTGRES_USER=postgres
POSTGRES_PASSWORD=build-placeholder

# Search Engine Configuration (build-time placeholders)
TYPESENSE_HOST=localhost
TYPESENSE_PORT=8108
TYPESENSE_PROTOCOL=http
TYPESENSE_API_KEY=build-placeholder-typesense-key

# Redis Configuration (build-time placeholder)
REDIS_URL=redis://localhost:6379

# Application Configuration
NODE_ENV=production
PM2_INSTANCES=max

# Optional Services (build-time placeholders)
GOTIFY_URL=https://gotify.example.com
GOTIFY_TOKEN=build-placeholder-gotify-token
ROMM_SERVER_URL=https://romm.example.com
ROMM_USERNAME=placeholder-user
ROMM_PASSWORD=placeholder-pass
N8N_WEBHOOK_URL=https://n8n.example.com/webhook/placeholder

# Public site URL
PUBLIC_SITE_URL=http://localhost:3000

# =============================================================================
# IMPORTANT NOTES:
# =============================================================================
# 1. These values are ONLY used during Docker build (`npm run build`)
# 2. Real environment variables are provided at runtime via docker-compose.yml
# 3. This file can be safely committed to git (no real secrets)
# 4. SvelteKit build process may need these vars available, even if not used
# 5. Runtime environment from .env.docker will override these values